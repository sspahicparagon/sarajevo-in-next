import WorkTime from "./WorkTime";

export default interface ElementInformation {
    Phone?: string;
    Adresa?: string;
    Website?: string;
    worktime?: WorkTime[];
    Name?: string;
    Latitude?: string;
    Longitude?: string;
}